<% if status.present? %>
  <% 
    schedule_status = ScheduleStatus.find_by(name: status)
    badge_class = schedule_status&.color_class || "bg-gray-100 text-gray-800"
    display_name = schedule_status&.display_name || status
  %>
  <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium <%= badge_class %>">
    <%= display_name %>
  </span>
<% else %>
  <span class="text-gray-400 text-xs">—</span>
<% end %>